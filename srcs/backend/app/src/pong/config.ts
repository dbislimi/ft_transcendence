// Centralized game configuration for Pong
export const DEFAULT_GAME_SPEED = 1; // multiplier for delta time
export const DEFAULT_MAX_SCORE = 2; // points to win a match
export const DEFAULT_BONUS_TIME = 1; // seconds between bonus spawns
export const DEFAULT_BONUS_RADIUS = 20; // visual radius for bonus collision
export const DEFAULT_COUNTDOWN_SECONDS = 3; // seconds for pre-game countdown
